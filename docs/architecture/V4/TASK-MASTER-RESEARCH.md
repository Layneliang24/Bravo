# Task-Master 子任务机制深度研究报告

> **研究时间**: 2025-10-24
> **研究目的**: 彻底理解Task-Master的子任务生成机制，为V4文档设计提供准确基础
> **模型**: Claude 3.5 Sonnet (claude-sonnet-4)

---

## 📋 执行摘要

**核心发现**: Task-Master **原生支持子任务功能**，通过AI驱动的`expand`命令自动生成。

**关键结论**:

- ✅ Task-Master有完整的子任务管理命令组
- ✅ 子任务通过AI自动生成，不是手动创建
- ✅ 子任务存储在tasks.json的subtasks数组中
- ⚠️ Task-Master CLI显示子任务存在限制（v0.25.1）

---

## 1️⃣ Task-Master 子任务功能架构

### 1.1 子任务管理命令组

从`task-master help`输出分析，Task-Master提供完整的子任务管理：

```bash
╭──────────────────────╮
│  Subtask Management  │
╰──────────────────────╯
add-subtask        --parent=<id> --title="<title>"          # 手动添加子任务
                   [--description="<desc>"]

add-subtask        --parent=<id> --task-id=<id>             # 转换现有任务为子任务

remove-subtask     --id=<parentId.subtaskId>                # 删除子任务
                   [--convert]                              # 可选：转回独立任务

clear-subtasks     --id=<id>                                # 清空指定任务的子任务
clear-subtasks     --all                                    # 清空所有任务的子任务
```

### 1.2 AI驱动的子任务生成

**核心命令**: `expand`

```bash
╭─────────────────────────────╮
│  Task Analysis & Breakdown  │
╰─────────────────────────────╯
expand             --id=<id>                               # 展开单个任务为子任务
                   [--num=5]                               # 生成子任务数量
                   [--research]                            # 启用研究增强
                   [--prompt="<context>"]                  # 额外上下文

expand --all       [--force]                               # 批量展开所有任务
                   [--research]                            # 使用Perplexity研究

analyze-complexity [--research]                            # 分析任务复杂度
                   [--threshold=5]                         # 复杂度阈值（1-10）
```

**工作流程**:

```
1. analyze-complexity  → 分析任务复杂度，识别需要展开的任务
2. expand --id=<id>    → AI生成子任务（基于任务描述和上下文）
3. subtasks自动保存   → 存入tasks.json的subtasks数组
```

---

## 2️⃣ 子任务数据结构

### 2.1 JSON存储格式

从alpha项目的实际tasks.json分析：

```json
{
  "feature-test_task_master": {
    "tasks": [
      {
        "id": 1,
        "title": "数据模型设计与数据库迁移",
        "description": "扩展现有Django模型...",
        "details": "基于现有Expression模型...",
        "testStrategy": "编写模型单元测试...",
        "priority": "high",
        "dependencies": [],
        "status": "done",
        "subtasks": [
          // ← 子任务数组
          {
            "id": 1, // 子任务ID（局部编号）
            "title": "扩展Expression模型创建IdiomaticExpression",
            "description": "基于现有Expression模型扩展...",
            "dependencies": [], // 子任务可以有依赖
            "details": "在backend/apps/english/models.py中...",
            "status": "done", // 子任务有独立状态
            "testStrategy": "编写模型单元测试..."
          },
          {
            "id": 2,
            "title": "创建ExpressionScenario和ExpressionSource模型",
            "description": "设计并实现ExpressionScenario...",
            "dependencies": [],
            "details": "创建ExpressionScenario模型...",
            "status": "done",
            "testStrategy": "测试模型间的多对多关系..."
          }
          // ... 更多子任务
        ]
      }
    ]
  }
}
```

### 2.2 子任务字段结构

**必需字段**:

- `id`: 子任务ID（在父任务内部编号，从1开始）
- `title`: 子任务标题
- `description`: 子任务描述
- `status`: 子任务状态（pending/in-progress/done/cancelled等）

**可选字段**:

- `dependencies`: 依赖的其他子任务ID数组（如`["2.1"]`表示依赖父任务2的子任务1）
- `details`: 实施细节（可以非常详细，包含`<info added>`时间戳记录）
- `testStrategy`: 测试策略
- `priority`: 优先级
- `complexity`: 复杂度

---

## 3️⃣ 子任务生成机制

### 3.1 AI如何生成子任务

**输入**:

1. 父任务的`title`、`description`、`details`
2. 可选的`--prompt`参数提供额外上下文
3. 项目上下文（通过.taskmaster/config.json配置的AI模型）
4. 可选的`--research`参数启用Perplexity AI研究

**处理**:

- 使用配置的LLM（Claude/GPT-4等）
- 分析任务复杂度和可分解性
- 生成逻辑上连贯的子任务序列
- 自动设置子任务间的依赖关系

**输出**:

- 生成的子任务直接添加到tasks.json
- 每个子任务有完整的字段（title/description/details等）
- 自动分配局部ID（1, 2, 3...）

### 3.2 复杂度分析驱动展开

```bash
task-master analyze-complexity --threshold=5
```

- 分析所有任务的复杂度（1-10分）
- 生成复杂度报告（存储在.taskmaster/reports/）
- 推荐需要展开的任务（复杂度 >= 阈值）
- 可选：使用`--research`进行更深入分析

**示例输出**:

```
Task #3: "智能数据采集系统开发"
  Complexity Score: 8/10
  Recommendation: HIGH - Should be expanded into subtasks
  Suggested Subtasks: 6-8
  Reasoning: Complex multi-source integration...
```

---

## 4️⃣ 子任务命令详解

### 4.1 expand - AI生成子任务

**基础用法**:

```bash
# 展开单个任务，生成5个子任务（默认）
task-master expand --id=3

# 指定生成子任务数量
task-master expand --id=3 --num=8

# 添加额外上下文
task-master expand --id=3 --prompt="需要考虑现有爬虫架构的兼容性"

# 使用研究增强（调用Perplexity AI）
task-master expand --id=3 --research

# 强制重新展开（覆盖现有子任务）
task-master expand --id=3 --force
```

**批量展开**:

```bash
# 展开所有pending状态任务
task-master expand --all

# 基于复杂度报告批量展开
task-master analyze-complexity --threshold=6
task-master expand --all  # 只展开推荐的任务
```

### 4.2 add-subtask - 手动添加子任务

**创建新子任务**:

```bash
task-master add-subtask \
  --parent=3 \
  --title="实现反爬虫机制" \
  --description="添加请求限流和User-Agent轮换" \
  --dependencies="3.1,3.2" \
  --status="pending"
```

**转换现有任务为子任务**:

```bash
# 将任务#15转换为任务#3的子任务
task-master add-subtask --parent=3 --task-id=15
```

### 4.3 update-subtask - 更新子任务

```bash
# 追加信息到子任务（带时间戳）
task-master update-subtask \
  --id=3.2 \
  --prompt="已完成CambridgeCrawler实现，测试通过率95%"
```

**效果**:

```json
{
  "details": "原有details内容\n<info added on 2025-10-24T10:30:15.123Z>\n已完成CambridgeCrawler实现，测试通过率95%\n</info added>"
}
```

### 4.4 remove-subtask - 删除/转换子任务

```bash
# 删除子任务
task-master remove-subtask --id=3.5

# 删除并转换回独立任务
task-master remove-subtask --id=3.5 --convert
```

### 4.5 clear-subtasks - 批量清空

```bash
# 清空指定任务的所有子任务
task-master clear-subtasks --id=3

# 清空所有任务的子任务（慎用）
task-master clear-subtasks --all
```

---

## 5️⃣ 子任务状态管理

### 5.1 状态类型

子任务支持的状态（与主任务相同）:

- `pending`: 待处理
- `in-progress`: 进行中
- `done`: 已完成
- `review`: 审查中
- `blocked`: 被阻塞
- `deferred`: 延期
- `cancelled`: 已取消

### 5.2 设置子任务状态

```bash
# 使用主任务的set-status命令，但ID格式为 parentId.subtaskId
task-master set-status --id=3.2 --status=in-progress
task-master set-status --id=3.2 --status=done
```

### 5.3 Dashboard统计

```bash
task-master list
```

输出包含子任务进度统计：

```
╭─────────────────────────────────────────────────────────────╮
│   Subtasks Progress: ████████░░░░░░░░░░░░░░░░░░░░ 40%      │
│   Completed: 12/30                                          │
│   In Progress: 3  Pending: 15  Blocked: 0  Cancelled: 0    │
╰─────────────────────────────────────────────────────────────╯
```

### 5.4 列表显示子任务

```bash
# 默认不显示子任务
task-master list

# 包含子任务显示
task-master list --with-subtasks
```

---

## 6️⃣ 子任务依赖管理

### 6.1 依赖关系表示

**跨主任务依赖**:

```json
{
  "id": 5,
  "title": "API开发",
  "dependencies": [2, 3], // 依赖任务2和3
  "subtasks": []
}
```

**子任务间依赖**:

```json
{
  "id": 3,
  "subtasks": [
    { "id": 1, "dependencies": [] },
    { "id": 2, "dependencies": ["3.1"] }, // 依赖本任务的子任务1
    { "id": 3, "dependencies": ["3.1", "3.2"] } // 依赖多个子任务
  ]
}
```

**跨任务子任务依赖**（理论支持）:

```json
{
  "id": 5,
  "subtasks": [
    { "id": 1, "dependencies": ["3.4", "4.2"] } // 依赖任务3的子任务4和任务4的子任务2
  ]
}
```

### 6.2 依赖验证

```bash
# 验证依赖关系有效性（包括子任务）
task-master validate-dependencies

# 自动修复无效依赖
task-master fix-dependencies
```

---

## 7️⃣ 实际使用观察

### 7.1 CLI显示限制（v0.25.1）

**问题**: 使用`task-master show <id>`查看任务时，即使JSON中有subtasks，也显示：

```
╭──────────────────────────────────────────────────────╮
│ No subtasks found. Consider breaking down this task: │
│ Run: task-master expand --id=1                       │
╰──────────────────────────────────────────────────────╯
```

**原因分析**:

1. **可能是版本Bug**: v0.25.1存在显示问题，v0.30.0可能已修复
2. **Tag上下文问题**: 子任务可能存储在不同的tag中
3. **subtasks数组为空**: 尽管外层有subtasks字段，但可能是空数组

**验证方法**:

```bash
# 直接查看JSON文件
cat .taskmaster/tasks/tasks.json | grep -A 50 '"id": 1,'

# 使用--with-subtasks参数
task-master list --with-subtasks
```

### 7.2 子任务的实际生成流程

从alpha项目的tasks.json观察到：

1. **任务1**: "数据模型设计与数据库迁移"

   - 有5个子任务（1.1 - 1.5）
   - 每个子任务都有详细的`details`字段
   - 包含`<info added>`时间戳更新记录

2. **子任务details非常详细**:

   - 包含具体实现步骤
   - 记录技术决策
   - 追踪进度更新（通过`<info added>`标签）
   - 包含测试验证结果

3. **AI生成质量高**:
   - 子任务逻辑连贯
   - 依赖关系合理
   - 粒度适中（每个子任务是独立可完成的单元）

---

## 8️⃣ V4文档设计影响

### 8.1 原设计的错误假设

**❌ 错误假设**:

- 认为Task-Master不支持子任务
- 设计了"适配层"来"推断"和"生成"子任务
- 过度复杂化了集成逻辑

**✅ 正确理解**:

- Task-Master原生支持AI驱动的子任务生成
- 不需要"推断"子任务，直接使用`expand`命令
- 适配层应该专注于格式转换和项目集成

### 8.2 适配层职责重新定义

**适配层应该做的**:

1. **格式转换**: Task-Master JSON ↔ 项目文件结构
2. **目录组织**: 自动创建符合项目规范的文件/目录
3. **文件关联**: 关联PRD章节、测试文件、实现文件
4. **双向同步**: Task-Master ↔ 项目文件系统状态同步
5. **规范映射**: Task-Master任务ID → 项目REQ-ID/TASK-ID映射
6. **Git集成**: 自动创建feature分支、关联commit

**适配层不应该做的**:

1. ❌ 推断或生成子任务（Task-Master的expand命令已完成）
2. ❌ 复杂的任务分解逻辑（AI已处理）
3. ❌ 重复Task-Master的依赖管理

### 8.3 工作流程重新设计

**正确的工作流**:

```
1. PRD编写
   ↓
2. task-master parse-prd --input=PRD.md
   → 生成主任务列表
   ↓
3. task-master analyze-complexity --threshold=5
   → AI分析任务复杂度
   ↓
4. task-master expand --all --research
   → AI自动生成子任务
   ↓
5. 适配层介入
   → 格式转换: tasks.json → 项目文件结构
   → 目录创建: 为每个任务/子任务创建对应目录
   → 文件关联: 关联PRD章节、创建测试模板
   ↓
6. 开发者开始工作
   task-master next  → 获取下一个任务
   ↓
7. 适配层同步
   → 文件系统变化 → 更新Task-Master状态
   → Task-Master状态变化 → 更新Git/PRD
```

---

## 9️⃣ 关键命令速查

### 9.1 子任务生成

```bash
# 1. 从PRD生成任务
task-master parse-prd --input=docs/PRD/feature-xxx.md --research

# 2. 分析复杂度
task-master analyze-complexity --threshold=5 --research

# 3. 查看分析报告
task-master complexity-report

# 4. 批量展开任务为子任务
task-master expand --all --research

# 5. 单个任务展开
task-master expand --id=3 --num=6 --prompt="考虑现有架构"
```

### 9.2 子任务管理

```bash
# 查看所有任务（包含子任务）
task-master list --with-subtasks

# 查看具体任务详情
task-master show 3

# 设置子任务状态
task-master set-status --id=3.2 --status=in-progress

# 更新子任务进度
task-master update-subtask --id=3.2 --prompt="已完成XX，测试通过"

# 手动添加子任务
task-master add-subtask --parent=3 --title="子任务标题"

# 删除子任务
task-master remove-subtask --id=3.5
```

### 9.3 依赖管理

```bash
# 验证依赖（包括子任务）
task-master validate-dependencies

# 修复无效依赖
task-master fix-dependencies

# 添加依赖
task-master add-dependency --id=3.2 --depends-on=3.1

# 删除依赖
task-master remove-dependency --id=3.2 --depends-on=3.1
```

---

## 🔟 更新建议

### 10.1 V4文档需要修改的部分

**PART2-TM-ADAPTER.md**:

- 删除"推断子任务"相关描述
- 明确说明Task-Master原生子任务支持
- 重新定义适配层职责：格式转换、目录组织、文件关联、双向同步
- 添加Task-Master命令使用示例

**PART6-IMPL.md**:

- 修正实现步骤，使用`task-master expand`而非适配层生成
- 添加复杂度分析步骤（`analyze-complexity`）
- 提供完整的命令序列示例

**APPENDIX-QA.md**:

- 更新关于子任务的问答
- 明确AI生成机制
- 解释适配层的真实作用

### 10.2 需要验证的问题

1. ✅ **Task-Master是否支持子任务**? → 是，原生支持
2. ✅ **子任务如何生成**? → AI驱动的`expand`命令
3. ✅ **子任务数据结构**? → subtasks数组，完整字段
4. ⚠️ **CLI显示问题**? → v0.25.1可能有bug，建议升级到v0.30.0
5. ❓ **适配层真实需求**? → 格式转换和项目集成，不是生成子任务

---

## 📚 参考资料

- Task-Master GitHub: https://github.com/eyaltoledano/claude-task-master
- Alpha项目实际案例: `/s/WorkShop/cursor/alpha/.taskmaster/tasks/tasks.json`
- Task-Master版本: 0.25.1 (建议升级到 0.30.0)

---

## ✅ 研究结论

1. **Task-Master完全支持子任务功能**，通过AI自动生成，无需适配层"推断"
2. **V4文档的适配层设计需要重构**，专注于格式转换和项目集成
3. **工作流程需要简化**，直接使用Task-Master的expand命令
4. **文档质量不会受影响**，反而会更准确、更实用

**下一步行动**: 基于本研究报告，重新设计并更新V4文档体系。

---

**模型标识**: Claude 3.5 Sonnet (claude-sonnet-4)
