# Task-0真正的自动生成方案

> **文档日期**: 2025-01-15
> **问题**: 如何实现真正的自动生成（无需手动运行脚本）？

---

## 🎯 真正的自动生成方案

### ✅ 已实现：集成到task-master-parse-prd.sh脚本

**真正的自动化**：使用项目包装脚本 `scripts/task-master-parse-prd.sh`

该脚本会在parse-prd成功后**自动生成Task-0**，无需用户手动执行额外命令。

**使用方式**：

```bash
# 使用包装脚本（真正的自动化）
bash scripts/task-master-parse-prd.sh \
  docs/00_product/requirements/REQ-2025-003-user-login/REQ-2025-003-user-login.md \
  --tag REQ-2025-003-user-login

# 输出示例：
# [步骤1/6] 检查PRD文件...
# [步骤2/6] 验证PRD状态...
# [步骤3/6] 执行task-master parse-prd...
# [步骤4/6] 记录PRD路径到tasks.json metadata...
# [步骤5/6] 自动生成Task-0...  ⭐ 自动执行，无需手动
# [步骤6/6] 更新PRD状态...
```

**或创建alias后使用**：

```bash
# 创建alias（可选，更便捷）
alias tm-parse='bash scripts/task-master-parse-prd.sh'

# 然后正常使用
tm-parse <prd-file> --tag <tag-name>
```

---

## 🔧 工作原理

### 脚本执行流程

```
用户执行: bash scripts/task-master-parse-prd.sh <prd-file> --tag <tag>
  ↓
[步骤1/6] 检查PRD文件是否存在
  ↓
[步骤2/6] 验证PRD状态（必须在approved状态）
  ↓
[步骤3/6] 执行 task-master parse-prd（生成tasks.json）
  ↓
[步骤4/6] 记录PRD路径到tasks.json metadata
  ↓
[步骤5/6] ⭐ 自动生成Task-0
  ├─ 从--tag参数或PRD路径提取REQ-ID
  ├─ 调用 python scripts/task-master/adapter.py <REQ-ID>
  └─ Task-0自动插入到tasks列表第一位
  ↓
[步骤6/6] 更新PRD状态为implementing
  ↓
完成 ✅
```

### 自动检测REQ-ID

脚本会按以下顺序自动检测REQ-ID：

1. **从--tag参数提取**（优先级最高）

   ```bash
   --tag REQ-2025-003-user-login  # REQ-ID = REQ-2025-003-user-login
   ```

2. **从PRD文件路径提取**

   ```bash
   docs/00_product/requirements/REQ-2025-003-user-login/REQ-2025-003-user-login.md
   # 提取: REQ-2025-003-user-login
   ```

3. **从tasks.json查找最新更新的tag**（后备方案）
   ```python
   # 查找metadata.updated_at最新的tag
   ```

---

## ✅ 优势

### 真正的自动化

- ✅ **无需手动执行额外命令**：parse-prd后自动生成Task-0
- ✅ **集成到现有工作流**：使用现有的包装脚本，无需改变使用习惯
- ✅ **自动检测REQ-ID**：从参数或文件路径自动提取，无需手动指定
- ✅ **幂等性**：如果Task-0已存在，会跳过生成（不会重复）

### 用户体验

- ✅ **透明化**：用户只需使用包装脚本，无需了解Task-0生成细节
- ✅ **错误处理**：如果REQ-ID检测失败，会提示用户手动运行
- ✅ **非阻塞**：Task-0生成失败不会阻止整个流程（非阻塞错误）

---

## 📋 完整工作流示例

### 新PRD的标准流程（真正的自动化）

```bash
# 1. 使用包装脚本解析PRD（自动生成Task-0）
bash scripts/task-master-parse-prd.sh \
  docs/00_product/requirements/REQ-2025-004-new-feature/REQ-2025-004-new-feature.md \
  --tag REQ-2025-004-new-feature

# 输出：
# ✅ parse-prd执行成功
# ✅ Task-0已自动生成  ⭐ 自动完成，无需额外命令

# 2. 展开任务（Task-0已存在，无需额外操作）
task-master expand --all --tag REQ-2025-004-new-feature --research
```

---

## 🆚 对比：之前的"手动生成" vs 现在的"真正的自动生成"

### 之前的方案（手动生成）❌

```bash
# 1. 执行parse-prd
task-master parse-prd <prd-file> --tag <tag>

# 2. 手动执行额外命令生成Task-0（容易遗漏）
python scripts/task-master/adapter.py <REQ-ID>  # ❌ 需要记住这一步
```

**问题**：

- ❌ 需要手动执行额外命令
- ❌ 容易遗漏步骤
- ❌ 不是真正的自动化

### 现在的方案（真正的自动生成）✅

```bash
# 1. 使用包装脚本（自动生成Task-0）
bash scripts/task-master-parse-prd.sh <prd-file> --tag <tag>
# ✅ Task-0自动生成，无需额外命令
```

**优势**：

- ✅ 真正的自动化（无需手动执行额外命令）
- ✅ 不会遗漏步骤
- ✅ 用户体验好

---

## 📝 总结

### Task-0真正的自动生成

| 方案                 | 自动化程度    | 使用方式                                                       | 状态          |
| -------------------- | ------------- | -------------------------------------------------------------- | ------------- |
| **手动运行脚本**     | ❌ 手动       | `python scripts/task-master/adapter.py <REQ-ID>`               | ❌ 不推荐     |
| **包装脚本自动生成** | ✅✅ 真正自动 | `bash scripts/task-master-parse-prd.sh <prd-file> --tag <tag>` | ✅✅ **推荐** |

### 关键点

1. **使用包装脚本**：`scripts/task-master-parse-prd.sh`
2. **真正的自动化**：parse-prd后自动生成Task-0，无需手动执行额外命令
3. **自动检测REQ-ID**：从--tag参数或PRD路径自动提取
4. **幂等性**：如果Task-0已存在，会跳过生成

---

## 🔗 相关文档

- [Task-0固定检查任务说明](TASK0-FIXED-SUBTASKS.md)
- [Task-0生成时机说明](TASK0-GENERATION-TIMING.md)
- [Task-0自动生成触发方式](TASK0-AUTO-GENERATION.md)
