---
description: 规则描述（一句话说明规则用途）
globs: **/*.py, **/*.ts  # 文件匹配模式，如果不依赖文件类型，使用 **/*
priority: 500  # 优先级，数字越大优先级越高
# alwaysApply: true  # 仅在核心原则层使用，工作流程规则不要设置
---

# 规则标题

## 规则概述

简要说明规则的用途和适用范围。

## 核心原则

**关键规则**：明确说明规则的核心要求

## 应用场景

- 场景1：何时应用此规则
- 场景2：何时应用此规则

## 具体要求

### 1. 要求1

具体要求说明

### 2. 要求2

具体要求说明

## 禁止事项

- ❌ 禁止行为1
- ❌ 禁止行为2

## 参考规则

- `@.cursor/rules/workflows/{相关规则}.mdc`
- `@.cursor/rules/quality/{相关规则}.mdc`

## 相关文档

- `@docs/architecture/V4/{相关文档}.md`

---

## 规则应用声明

**当AI被路由到此规则时，必须在响应开头明确声明**：

```
[应用规则：@.cursor/rules/{规则路径}]
[切换到{角色名称}角色]（如果有角色要求）
```

**验证要求**：
- ✅ 必须明确声明应用了此规则
- ✅ 必须遵循本规则中的所有约束和流程
- ✅ 如果本规则定义了角色，必须切换到对应角色
- ✅ 不能跳过声明步骤直接执行操作
