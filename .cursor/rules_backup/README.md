# Cursor规则系统索引

## 📋 规则组织结构

本项目的Cursor规则系统采用**分层管理**策略：全局宪法 + 模块化领域规则。

### 目录结构

```
.cursor/
└── rules/
    ├── README.md                    # 本文件（规则索引）
    ├── lifecycle/                   # 生命周期阶段规则
    │   ├── prd-design.mdc          # PRD设计阶段
    │   ├── task-generation.mdc     # Task-Master生成任务阶段
    │   ├── task-execution.mdc      # 任务执行阶段
    │   ├── development.mdc         # 开发阶段
    │   ├── testing.mdc             # 测试阶段
    │   ├── debugging.mdc           # Debug阶段
    │   ├── pre-commit.mdc           # Pre-commit和本地通行证阶段
    │   ├── ci-cd.mdc                # CI/CD阶段
    │   └── deployment.mdc           # 部署阶段
    ├── roles/                       # 角色切换规则
    │   ├── developer.mdc            # 开发专家
    │   ├── tester.mdc              # 测试专家
    │   ├── architect.mdc           # 架构专家
    │   └── prd-designer.mdc        # PRD设计专家
    ├── v4/                          # V4核心规则
    │   ├── v4-core-principles.mdc  # V4核心原则
    │   ├── v4-traceability.mdc     # 追溯链规则
    │   └── v4-contract-driven.mdc  # 契约驱动规则
    ├── taskmaster/                  # Task-Master相关规则
    │   ├── taskmaster.mdc
    │   ├── dev_workflow.mdc
    │   └── hamster.mdc
    └── [技术栈规则]                 # 技术栈特定规则
        ├── django_split.mdc
        ├── vue_component.mdc
        ├── code_quality.mdc
        ├── test_coverage.mdc
        └── ...
```

## 🎯 规则优先级

规则按优先级自动加载（数字越大优先级越高）：

- **1000**: V4核心原则（alwaysApply: true）
- **950**: Pre-commit规则、PRD设计专家角色
- **900**: 架构专家角色、追溯链规则
- **850**: 测试专家角色、契约驱动规则、测试阶段规则
- **800**: 开发专家角色、任务生成阶段规则
- **750**: 开发阶段规则
- **700**: 任务执行阶段规则
- **650**: CI/CD阶段规则
- **600**: Debug阶段规则
- **550**: 部署阶段规则

## 📚 规则分类

### 1. 生命周期阶段规则

按项目开发的不同阶段组织规则：

| 阶段       | 规则文件                        | 触发条件            | 主要职责               |
| ---------- | ------------------------------- | ------------------- | ---------------------- |
| PRD设计    | `lifecycle/prd-design.mdc`      | 编辑PRD文件         | 精化需求，补充技术细节 |
| 任务生成   | `lifecycle/task-generation.mdc` | 编辑Task-Master文件 | 解析PRD，生成任务      |
| 任务执行   | `lifecycle/task-execution.mdc`  | 编辑代码文件        | 执行任务，遵循TDD      |
| 开发       | `lifecycle/development.mdc`     | 编辑代码文件        | 实现功能代码           |
| 测试       | `lifecycle/testing.mdc`         | 编辑测试文件        | 编写测试用例           |
| Debug      | `lifecycle/debugging.mdc`       | 所有文件            | 调试问题，定位根因     |
| Pre-commit | `lifecycle/pre-commit.mdc`      | 所有文件            | 提交前检查             |
| CI/CD      | `lifecycle/ci-cd.mdc`           | 编辑工作流文件      | CI/CD流程管理          |
| 部署       | `lifecycle/deployment.mdc`      | 编辑部署文件        | 部署流程管理           |

### 2. 角色切换规则

根据任务类型自动切换AI角色：

| 角色        | 规则文件                 | 触发条件         | 主要职责             |
| ----------- | ------------------------ | ---------------- | -------------------- |
| 开发专家    | `roles/developer.mdc`    | 执行代码实现任务 | 实现功能代码         |
| 测试专家    | `roles/tester.mdc`       | 执行测试编写任务 | 编写测试用例         |
| 架构专家    | `roles/architect.mdc`    | 执行架构设计任务 | 设计API契约，验证PRD |
| PRD设计专家 | `roles/prd-designer.mdc` | 执行PRD精化任务  | 精化需求文档         |

### 3. V4核心规则

V4架构的核心设计原则和约束：

| 规则文件                    | 说明                              |
| --------------------------- | --------------------------------- |
| `v4/v4-core-principles.mdc` | V4五条铁律、追溯链、契约驱动、TDD |
| `v4/v4-traceability.mdc`    | 追溯链格式、实现方式、验证规则    |
| `v4/v4-contract-driven.mdc` | 契约驱动工作流、Mock Server使用   |

### 4. 技术栈规则

针对特定技术栈的规则：

| 规则文件            | 说明               |
| ------------------- | ------------------ |
| `django_split.mdc`  | Django应用拆分规则 |
| `vue_component.mdc` | Vue组件开发规则    |
| `code_quality.mdc`  | 代码质量检查规则   |
| `test_coverage.mdc` | 测试覆盖率要求     |
| `security_scan.mdc` | 安全扫描规则       |

## 🔗 规则引用

规则文件之间可以相互引用：

**引用语法**: `@文件路径`

**示例**:

```markdown
参考: @.cursor/rules/lifecycle/testing.mdc
参考: @docs/architecture/V4/AI-WORKFLOW-V4-PART1-ARCH.md
```

## 🎨 规则编写规范

### Frontmatter格式

```yaml
---
description: 规则描述
globs: **/*.py, **/*.ts        # 触发条件（Glob模式）
alwaysApply: true              # 是否总是应用（可选）
priority: 900                  # 优先级（可选，默认500）
---
```

### 内容结构

1. **角色切换**（如果适用）
2. **核心规则**
3. **工作流程**
4. **示例代码**
5. **禁止事项**
6. **参考文档**

### 使用示例标签

```markdown
<example>
// Good
function login(email: string, password: string) {
  return api.post('/auth/login', { email, password });
}

// Bad
function login(e: string, p: string) {
return fetch('/login', { body: JSON.stringify({e, p}) });
}
</example>
```

## 📖 快速导航

### 按场景查找规则

**我要设计PRD**:
→ `lifecycle/prd-design.mdc` + `roles/prd-designer.mdc`

**我要生成任务**:
→ `lifecycle/task-generation.mdc`

**我要执行开发任务**:
→ `lifecycle/task-execution.mdc` + `roles/developer.mdc` + `lifecycle/development.mdc`

**我要编写测试**:
→ `lifecycle/testing.mdc` + `roles/tester.mdc`

**我要调试问题**:
→ `lifecycle/debugging.mdc`

**我要提交代码**:
→ `lifecycle/pre-commit.mdc` + `compliance_workflow.mdc`

**我要部署**:
→ `lifecycle/deployment.mdc`

### 按技术栈查找规则

**Django开发**:
→ `django_split.mdc` + `lifecycle/development.mdc`

**Vue开发**:
→ `vue_component.mdc` + `lifecycle/development.mdc`

**测试编写**:
→ `lifecycle/testing.mdc` + `test_coverage.mdc`

## 🔄 规则维护

### 添加新规则

1. 确定规则分类（lifecycle/roles/v4/技术栈）
2. 创建`.mdc`文件，包含frontmatter
3. 编写规则内容，使用示例和引用
4. 更新本README.md，添加到相应分类

### 更新现有规则

1. 直接编辑对应的`.mdc`文件
2. 保持frontmatter格式一致
3. 更新相关引用

### 规则版本控制

- 所有规则文件纳入Git版本控制
- 重大变更应在规则文件中记录变更历史
- 保持向后兼容性

## 📚 参考文档

- **V4架构文档**: `docs/architecture/V4/AI-WORKFLOW-V4-*.md`
- **API契约指南**: `docs/01_guideline/api-contracts/README.md`
- **Task-Master文档**: `@.cursor/rules/taskmaster/taskmaster.mdc`
- **最佳实践**: https://github.com/PatrickJS/awesome-cursorrules
