name: Feature Branch - Development Validation

on:
  push:
    branches: [feature/*] # 只对feature分支触发

# 并发控制
concurrency:
  group: feature-push-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # 第一步：快速环境设置
  quick-setup:
    name: Quick Environment Setup
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install Dependencies
        run: |
          echo "📦 快速安装依赖..."
          npm ci --workspace=frontend
          cd backend && pip install -r requirements/test.txt

      - name: Environment Ready
        run: echo "✅ 开发环境准备完成"

  # 第二步：快速后端测试
  quick-backend-tests:
    name: Quick Backend Tests
    needs: quick-setup
    runs-on: ubuntu-latest
    timeout-minutes: 5

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_DATABASE: bravo_test
          MYSQL_USER: bravo_user
          MYSQL_PASSWORD: bravo_password
          MYSQL_ROOT_PASSWORD: root_password
        options: --health-cmd="mysqladmin ping -h 127.0.0.1 -P 3306 -u root -proot_password" --health-interval=10s --health-timeout=5s --health-retries=3
        ports:
          - 3306:3306

    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Install Backend Dependencies
        working-directory: ./backend
        run: |
          pip install -r requirements/test.txt

      - name: Run Quick Backend Tests
        working-directory: ./backend
        run: |
          echo "🧪 运行快速后端测试..."
          python -m pytest tests/ -v --maxfail=5 --tb=short
        env:
          DB_HOST: 127.0.0.1
          DB_PORT: 3306
          DB_NAME: bravo_test
          DB_USER: bravo_user
          DB_PASSWORD: bravo_password
          DJANGO_SETTINGS_MODULE: bravo.settings.test

  # 第三步：快速前端测试
  quick-frontend-tests:
    name: Quick Frontend Tests
    needs: quick-setup
    runs-on: ubuntu-latest
    timeout-minutes: 4
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install Frontend Dependencies
        run: |
          npm ci --workspace=frontend

      - name: Run Quick Frontend Tests
        run: |
          echo "🧪 运行快速前端测试..."
          npm run test:coverage-check --workspace=frontend

  # 第四步：代码质量快速检查
  quick-quality-check:
    name: Quick Quality Check
    needs: [quick-backend-tests, quick-frontend-tests]
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check Code Style
        run: |
          echo "🔍 快速代码风格检查..."

          # 检查Python代码风格
          if command -v flake8 &> /dev/null; then
            flake8 backend/ --max-line-length=120 --exclude=migrations,venv,.venv || echo "⚠️ 代码风格问题，建议修复"
          fi

          # 检查JavaScript代码风格
          if [ -f "frontend/.eslintrc.js" ]; then
            cd frontend && npm run lint || echo "⚠️ 前端代码风格问题，建议修复"
          fi

      - name: Check for Common Issues
        run: |
          echo "🔍 检查常见问题..."

          # 检查是否有调试代码残留
          if grep -r "console.log\|print(" --include="*.js" --include="*.vue" --include="*.py" frontend/ backend/; then
            echo "⚠️ 发现调试代码，建议清理"
          fi

          # 检查是否有TODO注释
          if grep -r "TODO\|FIXME\|HACK" --include="*.js" --include="*.vue" --include="*.py" frontend/ backend/; then
            echo "ℹ️ 发现TODO注释，请及时处理"
          fi

  # 第五步：开发反馈汇总
  development-feedback:
    name: Development Feedback
    runs-on: ubuntu-latest
    needs: [quick-backend-tests, quick-frontend-tests, quick-quality-check]
    if: always()
    steps:
      - name: Generate Development Report
        run: |
          echo "## 🚀 Feature Branch Development Feedback" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Branch:** ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
          echo "**Commit:** ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
          echo "**Author:** ${{ github.actor }}" >> $GITHUB_STEP_SUMMARY
          echo "**Message:** ${{ github.event.head_commit.message }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # 收集测试结果
          BACKEND_STATUS="${{ needs.quick-backend-tests.result }}"
          FRONTEND_STATUS="${{ needs.quick-frontend-tests.result }}"
          QUALITY_STATUS="${{ needs.quick-quality-check.result }}"

          echo "| Test Type | Status | Purpose |" >> $GITHUB_STEP_SUMMARY
          echo "|-----------|---------|---------|" >> $GITHUB_STEP_SUMMARY
          echo "| Backend Tests | $BACKEND_STATUS | Quick validation |" >> $GITHUB_STEP_SUMMARY
          echo "| Frontend Tests | $FRONTEND_STATUS | Quick validation |" >> $GITHUB_STEP_SUMMARY
          echo "| Quality Check | $QUALITY_STATUS | Code standards |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # 判断整体结果
          if [[ "$BACKEND_STATUS" == "success" && "$FRONTEND_STATUS" == "success" ]]; then
            echo "✅ **Development validation passed!**" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "🎉 Feature branch is ready for PR creation" >> $GITHUB_STEP_SUMMARY
            echo "📝 Next step: Create PR to dev branch" >> $GITHUB_STEP_SUMMARY

            if [[ "$QUALITY_STATUS" != "success" ]]; then
              echo "⚠️ Some quality issues detected - consider fixing before PR" >> $GITHUB_STEP_SUMMARY
            fi

            echo "Feature development validation completed successfully"
          else
            echo "❌ **Development validation failed!**" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "🔧 Please fix the failing tests before creating PR" >> $GITHUB_STEP_SUMMARY
            echo "💡 Check the test logs for detailed error information" >> $GITHUB_STEP_SUMMARY
            echo "Feature development validation failed"
            exit 1
          fi

      - name: Development Status Notification
        if: success()
        run: |
          echo "📍 Feature Branch Summary:"
          echo "   - Branch: ${{ github.ref_name }}"
          echo "   - Commit: ${{ github.sha }}"
          echo "   - Status: Ready for PR"
          echo "   - Next: Create PR to dev branch"
