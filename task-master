#!/bin/bash
# 项目本地task-master包装
# 自动拦截parse-prd命令并验证PRD状态

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
WRAPPER="$SCRIPT_DIR/scripts/task-master-wrapper.sh"

# 使用包装脚本
exec "$WRAPPER" "$@"

